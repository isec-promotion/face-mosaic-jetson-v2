[property]
gpu-id=0
enable=1
# NGCのモデル（ETLT）を使う
tlt-model-key=NVIDIA_TAO    # モデルカードのKeyに合わせてください（無い場合は空でOK）
tlt-encoded-model=./models/facedetectir/facedetectir.etlt
labelfile-path=./models/facedetectir/labels.txt

# INT8を使うなら
network-mode=1              # 0:FP32, 1:INT8, 2:FP16
int8-calib-file=./models/facedetectir/int8_calibration.txt

# 入力解像度（モデルカード記載の解像度に合わせる）
infer-dims=3;544;960        # 例。実際の "Input Resolution" に合わせて変更
uff-input-blob-name=input_1 # 形式により不要/名称異なる場合あり（ONNX/ETLTで差異）

batch-size=1
interval=0
gie-unique-id=1

# TAOモデル用のカスタムパーサ（DeepStream付属ライブラリ）
custom-lib-path=/opt/nvidia/deepstream/deepstream-7.1/lib/libnvds_infercustomparser.so
parse-bbox-func-name=NvDsInferParseCustomBatchedNMSTLT  # TAO DetectNet_v2系の標準NMSパーサ

[class-attrs-all]
pre-cluster-threshold=0.30
nms-iou-threshold=0.5
topk=100
