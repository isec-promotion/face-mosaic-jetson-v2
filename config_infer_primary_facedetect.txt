[property]
gpu-id=0
batch-size=1
network-type=0
num-detected-classes=1
network-mode=2                 # FP16
infer-dims=3;640;640;0        # モデル入力に合わせる

# どちらか片方（エンジンか元モデル）を正しく指定
# 既に engine を用意したなら：
# model-engine-file=./models/facedetect.engine

# エンジンが無い／自動生成したいなら（ETLT例）：
tlt-encoded-model=./models/tlt_facedetect_pruned_v1.0/resnet18_facedetect_pruned.etlt
tlt-model-key=nvidia_tlt
int8-calib-file=./models/tlt_facedetect_pruned_v1.0/cal.bin   # あれば
labelfile-path=./models/tlt_facedetect_pruned_v1.0/labels.txt

# TLT/TAO 用パーサ
custom-lib-path=/opt/nvidia/deepstream/deepstream-7.1/lib/libnvds_infercustomparser.so
parse-bbox-func-name=NvDsInferParseCustomFaceDetect

# スケーリング・前処理など（サンプルに合わせる）
maintain-aspect-ratio=1
